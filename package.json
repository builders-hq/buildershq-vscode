{
  "name": "buildershq",
  "displayName": "BuildersHQ",
  "description": "Track your developer presence across workspaces. Sends heartbeat signals to a self-hosted BuildersHQ server to show your active, idle, or away status in real time.",
  "version": "1.0.0",
  "publisher": "appmakers",
  "license": "MIT",
  "icon": "icon.png",
  "keywords": [
    "presence",
    "activity",
    "tracking",
    "developer",
    "productivity",
    "claude",
    "codex",
    "git",
    "status"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/builders-hq/buildershq-vscode"
  },
  "galleryBanner": {
    "color": "#1e1e2e",
    "theme": "dark"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "buildershq.pause",
        "title": "BuildersHQ: Pause Presence Tracking"
      },
      {
        "command": "buildershq.resume",
        "title": "BuildersHQ: Resume Presence Tracking"
      },
      {
        "command": "buildershq.loginWithGitHub",
        "title": "BuildersHQ: Login with GitHub"
      },
      {
        "command": "buildershq.logoutFromGitHub",
        "title": "BuildersHQ: Logout from GitHub"
      }
    ],
    "configuration": {
      "title": "BuildersHQ",
      "properties": {
        "buildershq.serverUrl": {
          "type": "string",
          "default": "http://127.0.0.1:3000/api/presence",
          "description": "URL of the BuildersHQ presence API endpoint. Defaults to localhost for self-hosted setups."
        },
        "buildershq.idleAfterSeconds": {
          "type": "number",
          "default": 300,
          "minimum": 10,
          "description": "Seconds of inactivity before status becomes idle."
        },
        "buildershq.awayAfterSeconds": {
          "type": "number",
          "default": 900,
          "minimum": 30,
          "description": "Seconds of inactivity before status becomes away."
        },
        "buildershq.heartbeatActiveSeconds": {
          "type": "number",
          "default": 30,
          "minimum": 10,
          "description": "How often (in seconds) to send heartbeats while active."
        },
        "buildershq.heartbeatIdleSeconds": {
          "type": "number",
          "default": 60,
          "minimum": 30,
          "description": "How often (in seconds) to send heartbeats while idle."
        },
        "buildershq.gitCommits.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Track git commits as developer activity."
        },
        "buildershq.claudeCode.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable Claude Code activity tracking (watches JSONL transcript files)."
        },
        "buildershq.claudeCode.transcriptPath": {
          "type": "string",
          "default": "",
          "description": "Override path to Claude Code transcript directory. Leave empty for auto-detection."
        },
        "buildershq.codex.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable OpenAI Codex activity tracking (watches JSONL session files)."
        },
        "buildershq.codex.transcriptPath": {
          "type": "string",
          "default": "",
          "description": "Override path to OpenAI Codex sessions directory. Leave empty for auto-detection (~/.codex/sessions)."
        },
        "buildershq.sharedEnvFilePath": {
          "type": "string",
          "default": "",
          "description": "Optional path to a .env file supplying PRESENCE_SERVER_URL, GITHUB_CLIENT_ID, GITHUB_CLIENT_SECRET, and MONGODB_URI. PRESENCE_SERVER_URL overrides the serverUrl setting. Leave empty to skip."
        }
      }
    }
  },
  "scripts": {
    "compile": "npm run check-types && node esbuild.js",
    "check-types": "tsc --noEmit",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "version:bump": "npm version patch --no-git-tag-version",
    "vscode:prepublish": "npm run check-types && node esbuild.js --production",
    "package": "npm run version:bump && npm run check-types && node esbuild.js --production"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.25.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "mongodb": "^6.21.0"
  }
}
